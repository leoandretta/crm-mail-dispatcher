{
    "name": "crm-mail-dispatcher",
    "description": "",
    "version": "1.0.0",
    "private": true,
    "workspaces": ["packages/*"],
    "scripts": {
        "dev": "concurrently --raw \"yarn workspace api dev\" \"yarn workspace web dev\"",
        "lint": "concurrently \"yarn workspace api lint\" \"yarn workspace web lint\""
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}
